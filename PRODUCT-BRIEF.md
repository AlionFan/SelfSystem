# 个人记录与计划系统：产品评估

日期：2026-09-06。本文保留初始产品评估与取舍；首版现已完成开发并部署至 https://me.joybeat.cn 。实际功能、验收结果和待完成的设备操作见 [部署记录](deploy/DEPLOYED.md)，下文设计建议不等于已交付清单。

## 判断

建议先做一个小范围、可实际使用的版本，验证“随手记下 → 整理 → 安排 → 完成或回看”是否比当前习惯顺手。自建的价值应来自个人工作流，而不是拥有更多功能。首版应优先投入快速记录、保存可靠性、跨设备同步和找回内容。

## 已知需求与待验证假设

- 用户希望记录日常想法、规划日程、管理待办，并在电脑和手机上使用。
- 已使用北京腾讯云服务器，不使用 Vercel。服务器到期后不续费，到期时间为 2027-06-16 00:18:22（北京时间），通过现有 SSH 别名 joybeat 管理，部署支持迁移。
- 已通过腾讯云 DNSPod 配置 `me.joybeat.cn`，备案号为京ICP备2026054579号，已在首页底部展示并链接工信部。
- 暂按仅本人使用、以中文文字记录为主评估；多人协作和附件需求尚未确认。
- 最新列出的使用环境为 MacBook、安卓手机、台式机 Windows、同一台式机 Ubuntu，按四套独立系统授权；此前提到 iPhone，也保留其兼容需求，是否仍纳入首批设备尚未明确。
- 用户已确定采用客户端证书（mTLS）：各系统独立凭证、一次性安装、日常免输入网站密码；全部页面及数据接口均受保护。私有网络、浏览器配对和通行密钥不作为首版访问方案。
- 用户希望锁屏后收到提醒，允许偶尔延迟；锁屏提醒纳入第一版。具体机型、浏览器和可接受延迟范围尚未确认。
- 当前记录工具、每天使用频率、最常见的遗漏情境、可接受的维护成本还没有事实依据。
- 用户尚未说明现有工具具体哪里不顺手。下一步设计应选择三个真实情境，比较当前做法与新流程，避免开发出另一个需要重复录入的入口。

## 产品应解决的三个问题

1. 有想法时可以立即保存，不必先填标题、选分类或决定执行日期。
2. 开始一天时能看到固定日程和主动选出的少量重点任务。
3. 几天或几个月后，仍能通过中文关键词、日期和关联任务找到原来的内容。

理想体验：在手机上记录一句话，回到电脑后可以整理成笔记，或从中创建关联任务并安排时间；原始想法始终保留。网络不佳时仍能留住草稿，系统明确告诉用户内容是否已经上传。

## 信息规则

| 内容 | 含义 | 示例 | 规则 |
| --- | --- | --- | --- |
| 想法或笔记 | 值得保留，不一定要执行 | 一个文章选题 | 不强制截止时间或完成状态 |
| 待办 | 可执行的下一步 | 写出文章提纲 | 可没有日期；可以完成、取消、归档 |
| 日程 | 占用一段确定时间 | 周三 14:00–15:00 讨论 | 有开始与结束时间；支持全天事项 |

提供统一快速输入入口，允许稍后整理类型。从笔记创建任务时保留来源关系，避免把原始记录直接覆盖。

待办的“计划执行日期”“截止日期”“提醒时间”分开表达。例如周五截止、周三准备、周三上午提醒。界面按需展开字段，避免每次输入都填写完整表单。

“今天”包括今天的日程和用户选定的任务。逾期任务独立折叠展示，并支持重新安排、取消和归档，不默认把所有历史欠账铺满首页。日期默认采用 Asia/Shanghai，全天事项按日历日期处理。

## 第一版范围

| 能力 | 第一版交付 | 验收方式 |
| --- | --- | --- |
| 快速记录 | 全局新增入口、正文直接输入、自动草稿、收件箱 | 已登录且应用可用时，从打开到保存一句话目标不超过 10 秒 |
| 想法整理 | 编辑、归档、少量可选标签、创建关联任务 | 从原记录能找到任务，从任务能回到原记录 |
| 待办 | 完成、取消、简单排序、可选执行日期和截止日期 | 无日期任务不会误判为逾期；执行日期不改变截止日期 |
| 今日安排 | 今天的固定日程和少量重点任务 | 新的一天可以迅速判断何时有空、下一步做什么 |
| 计划 | 待办列表、日程新增、未来七天议程 | 手机上无需拖拽即可修改日期或时间 |
| 查找 | 中文关键词搜索、日期筛选 | 用部分中文词语找回旧记录，结果涵盖笔记和待办 |
| 保存与同步 | 云端持久化、本地草稿与待上传队列、保存状态、重试 | 断网新增后恢复网络并重开应用，内容可同步；重复重试不重复创建 |
| 冲突保护 | 修改版本检查、保留冲突内容 | 两台设备修改同一条内容时不静默覆盖另一份修改 |
| 私密访问 | 逐台授权、日常免网站密码、设备撤销和恢复、保护全部数据接口 | 未授权设备不能读取应用内容或接口；撤销设备后无法再次访问云端 |
| 数据退出能力 | 回收站、JSON 完整导出与导入、独立备份 | 导出后能恢复笔记、待办、日程及其关系；执行一次备份恢复验证 |
| 手机入口 | 响应式网页、可添加到手机主屏幕 | 在实际使用的手机和电脑上验证记录、查找和编辑 |
| 锁屏提醒 | 服务端定时触发、设备通知授权、测试通知、选择接收设备 | 在 iPhone 和安卓的实际浏览器、移动网络和 Wi-Fi 下测试关闭页面后的接收情况，记录漏达和延迟 |

这些是最初的设计与验收目标，实际完成情况以部署记录为准。离线草稿、新增、页面重开及恢复后补传已验收；关闭应用后不保证立即上传。已有缓存和设备可用空间是离线工作的前提。

首版先采用简单纯文本编辑，输入框兼容手机系统语音听写。图片、录音、富文本嵌入和附件存储，等真实使用证明必要后再增加。

## 设备上的不同重点

- 手机：主要承担捕捉。新增按钮放在拇指容易触达的位置，允许不填标题保存，长内容自动保留草稿，不依赖悬停或拖拽操作。
- 电脑：主要承担整理和规划。支持更清晰的并排信息、键盘操作和搜索；可以完整编辑手机创建的内容。
- 建议导航为“今天、收件箱、计划、记录”，新增入口始终可见。“收件箱”表示待整理内容，“记录”保存已整理的笔记与归档内容，计划中仍保留全部待办入口。

## 需要批判和控制的风险

### 1. 自建系统可能增加管理负担

只实现记录、日历和待办，未必足以证明自建比现有习惯更好。关键验证是是否减少重复录入、是否更容易开始下一步，以及是否能更快找回旧内容。复杂分类、属性和统计会增加维护成本，首版允许内容先进入收件箱，再用短时间定期整理。

### 2. 托管位置与迁移

当前候选改为已有的北京腾讯云服务器。预计能减少网页访问的跨境链路，但需要实测，不能据此保证手机推送通道也可达。该服务器到期不续费，因此它是第一阶段的托管位置，应用和数据不能依赖其生命周期。

先用实际部署的最小页面和保存接口，在用户常用的手机移动网络和电脑宽带上测试设备授权、打开、保存及搜索；不能仅验证首页能打开。字体和关键资源尽量随应用提供，减少外部访问依赖。用户已确认备案完成；部署时核实腾讯云接入状态和域名配置，不重复要求用户办理已完成的备案。[腾讯云接入备案说明](https://cloud.tencent.com/document/faq/243/43878)

### 3. 添加到主屏幕不等于提醒可靠

PWA 是能够安装到主屏幕、按需实现离线能力的网页应用。WebKit 的 iOS Web Push 要求将网页添加到主屏幕，并由用户通过主动操作允许通知；相关支持从 iOS/iPadOS 16.4 开始。[WebKit 官方说明](https://webkit.org/blog/13878/web-push-for-web-apps-on-ios-and-ipados/)

用户已经要求锁屏提醒，因此必须先在实际手机、浏览器和网络上测试后台接收，再确定通知服务和调度方案。将提醒验证提前到正式界面开发之前，必要时评估与系统日历配合。

Web Push 的消息经由浏览器供应商的推送服务发送，网页服务端不能自行决定替换该通道；设备离线时消息可能排队，直到设备上线或消息过期。因此网页能访问，并不能证明同一设备上的通知能收到。[web.dev 推送机制说明](https://web.dev/articles/push-notifications-overview?hl=en)

提醒应由服务器保存并触发，不依赖网页保持打开或前端倒计时。需要处理任务改期、取消、完成后的待发送提醒，避免重复投递；已经交给推送通道的通知未必能撤回，应限制有效期。允许选择接收提醒的设备，避免所有设备反复响铃。提供测试通知与权限状态；服务端“已提交推送”不能直接显示为“手机已收到”。允许偶尔延迟不等于接受长期漏达，具体容忍延迟和离线补发规则在实测时确定。

使用自有服务器后，提醒可由应用配套的后台进程调度，并在重启后恢复尚未发送的提醒。迁移时必须避免新旧服务器同时发送。服务器仍需能访问实际浏览器使用的推送服务，部署北京不会自动消除该链路的不确定性。

### 4. 多设备同步不等于备份

手机和电脑可以同时修改同一内容，网络恢复也可能重复发送请求。需要修改版本、冲突内容保留、唯一创建标识和删除保护。同步可能把误删扩散到全部设备，因此还需独立备份和恢复能力。

浏览器数据可能被清理或因存储压力被回收，本地缓存不能作为唯一长期存储。[MDN 浏览器存储与回收机制](https://developer.mozilla.org/en-US/docs/Web/API/Storage_API/Storage_quotas_and_eviction_criteria)

### 5. 私密性应体现在数据访问上

域名难猜和禁止搜索引擎索引都不能替代设备鉴权。按用户最新要求关闭公开注册，首批设备通过受控方式逐台授权；MacBook、安卓、Windows、Ubuntu 各自独立凭证，支持逐台撤销与失去全部设备后的恢复。正文不写入运行日志。这里的默认方案是有访问控制的云端保存；如果用户要求服务提供方也无法看到内容，需要单独评估端到端加密与搜索、恢复之间的取舍。

“识别设备”实际是验证该系统持有的密钥或证书，并不能证明使用者一定是本人。应避免复制密钥到其他设备；普通可导出的密钥不能承诺绝对绑定某块物理硬件。撤销访问也不能远程收回设备此前下载的离线内容。

### 6. 费用和维护不能只看网页托管

总成本还包括数据库、备份、文件存储，以及未来可能需要的消息调度和 AI 服务。首版不为可选功能增加外部服务；上线前检查实际套餐、额度、休眠或暂停行为和备份保留策略，给出可审核的费用预估。当前没有足够信息承诺完全免费。

## 已确定的访问方案：客户端证书（mTLS）

每套系统各有一份证书及对应私钥，首批分别标记为 MacBook、Android、Windows、Ubuntu。日常打开 me.joybeat.cn 时，由浏览器提供身份凭证，无需输入网站密码；可能出现证书选择或系统解锁提示，具体体验以实际浏览器和主屏幕应用测试为准。

部署入口校验客户端证书及有效期，应用同时检查设备授权是否仍有效。全部页面、静态应用资源、搜索、导出、附件和通知订阅接口均走该入口；应用和数据库端口不直接对公网开放。应用只信任受控代理传入的已验证设备身份，代理覆盖外部同名请求头，避免绕过设备检查。[NGINX 客户端证书验证](https://nginx.org/en/docs/http/ngx_http_ssl_module.html)

证书的管理方式：

1. 首批签发通过服务器管理通道或独立签发工具完成，不开放匿名设备注册。设备私钥尽可能在本机生成并保留，向签发端提交证书请求；若兼容性要求使用导入包，使用受保护的交付方式，每台分别生成。
2. 保存设备名称、凭证标识、有效期和授权状态，允许逐台查看与撤销。签发权限单独保护，不把签发私钥交给普通网页进程。
3. 撤销同时更新设备授权记录和证书撤销机制，每次受保护请求检查设备状态，并终止该设备已有的实时连接；停用该设备的待发送通知。已交付到推送服务的消息和已下载的离线内容不能保证收回。
4. 证书到期前可更新，重装系统或丢失凭证后重新签发；旧凭证退出授权。迁移保留授权记录和信任配置，不因换服务器默认要求所有设备重新安装证书。
5. 保留独立于网站证书的服务器管理与恢复途径，避免所有客户端凭证失效后无法管理系统。

四端兼容性验证应先于完整业务界面：有效证书能访问；无证书、其他签发方证书、过期和已撤销证书均不能读取内容；保持连接时撤销也能阻止后续访问；重启浏览器与服务器后行为一致。手机另外测试从主屏幕打开、后台获取数据及点击通知回到页面，不能只用桌面浏览器通过作为完成依据。

客户端证书用于访问身份，网站自身仍使用 me.joybeat.cn 的常规 HTTPS 服务器证书。初次导入凭证可能需要系统解锁或一次性的导入保护码，这不新增一套日常网站账户密码。

## 域名与可迁移部署

固定使用 `me.joybeat.cn`，域名指向北京腾讯云服务器的 HTTPS 入口，全部应用内容由客户端证书保护。部署前检查现有域名记录，不覆盖其他项目。

实际部署复用了现有 Traefik 的 HTTP-01 自动签发与续期，仅 ACME 挑战路径由代理处理，应用仍要求客户端证书。初始评估的 DNS-01 方案未启用，也没有为此新增 DNS API 密钥。[Let's Encrypt 验证方式](https://letsencrypt.org/docs/challenge-types/)

应用采用可在普通 Linux 服务器启动的部署包，数据库、附件与运行环境分离，提醒调度与应用一起迁移。优先使用通用数据库和部署方式，不把功能绑定到特定云厂商。

每日备份应写到当前服务器以外的位置，并定期验证恢复。服务器到期前预留迁移窗口，建议至少提前一个月演练；这是一项计划建议，尚未设置自动提醒。迁移包含数据库、附件、授权记录、配置、证书与必要密钥、待发送提醒。采用受保护的转移方式，并停用旧服务器的调度，避免重复通知。保留域名，切换到新服务器并验证原有设备凭证仍有效。

下一台服务器或常开设备尚未确定，到期后持续在线仍需要长期的运行位置。台式机双系统切换、关机和断网会中断服务，不能未经验证就把它当作稳定替代。

## 后续取舍

- 待首版使用后再决定：重复任务、图片附件、系统分享入口、日历导出或订阅、AI 辅助拆分任务和摘要。锁屏提醒已确认纳入首版；若 Web Push 实测不符合需求，日历或其他通知通道需提前评估。
- 首版暂缓：多人协作、复杂项目管理、知识图谱、自定义数据库、习惯积分和 AI 自动调整日程。它们不是当前已陈述需求的必要条件。
- AI 若后续加入，先生成可编辑建议，由用户决定是否创建或改动任务；保留原始记录。

## 推荐推进顺序

1. 获取服务器连接方式、系统与配置、准确到期日、DNS 服务商；备案已由用户确认完成，部署时核实腾讯云接入状态，并选三个真实情境作为验证样例。
2. 在 me.joybeat.cn 的目标环境验证四套系统的免网站密码访问、未授权设备拒绝访问、设备撤销与恢复；同期验证手机锁屏提醒。
3. 完成上述第一版，在真实生活中试用两周。
4. 用实际结果决定继续投入：是否比旧习惯更快记录、是否少了重复输入、是否能找回旧内容，以及出现过多少保存或同步异常。

继续投入的条件：愿意把真实记录放进系统，核心记录流程足够方便、数据可靠性通过验证，且两类手机的锁屏提醒达到双方约定的延迟与漏达要求。若记录入口比现有方式更慢，先改入口；若境内访问和同步不稳定，先改部署与保存策略；若锁屏提醒实测不合格，先重新评估提醒架构。

> 历史记录：证书版已于 2026-09-06 被账号版替代，以下不是当前使用说明。

# Me 首版部署记录

交付日期：2026-09-06。正式入口：https://me.joybeat.cn 。

## 已部署

| 项目 | 实际配置 |
| --- | --- |
| 主机 | 腾讯云北京，SSH 别名 joybeat，82.156.0.31 |
| 到期 | 2027-06-16 00:18:22，Asia/Shanghai |
| DNS | 腾讯云 DNSPod，me 的 A 记录指向当前服务器，TTL 600 |
| Coolify 项目 | Me Personal，aa2nfkgag4jcnsfyntk3eivx |
| Coolify 服务 | joybeat-me，c6zlqam9byswlqx825cse0bc |
| 应用容器 | me-c6zlqam9byswlqx825cse0bc |
| 镜像 | joybeat-me:1.0.0 |
| 数据 / 配置 | /data/joybeat-me/data、/data/joybeat-me/config |
| 独立代理配置 | /data/coolify/proxy/dynamic/me.yaml |
| 备案 | 京ICP备2026054579号，首页底部已展示并链接工信部 |

应用未向宿主机发布端口；证书保护覆盖页面、静态资源和 API。客户端证书签发私钥与各设备私钥仅保留在本机受保护目录；服务器只接收验证所需公钥和应用运行密钥。部署临时令牌已撤销，Coolify API 已恢复此前关闭状态。

## 已实现

快速记录、自动草稿、收件箱、笔记标签与关键词搜索、笔记关联待办、待办完成与重开、计划执行日和截止日、今日重点、日程起止与全天标记、按日期查看计划、归档、回收站与恢复、JSON 导入导出、跨设备同步和版本冲突处理、离线保存与补传、设备撤销、浏览器提醒订阅及测试发送。

## 备份

- 服务器每天北京时间 03:20 后五分钟内生成 SQLite 一致性快照、可移植 JSON，并打包配置和代理文件；保留最近 30 天。由 `joybeat-me-backup.timer` 执行，关机错过会在恢复后补执行。
- Mac 登录会话中的 `cn.joybeat.me.backup` 每六小时取回最新备份，启动时也执行。需要 Mac 在线并能通过已有 SSH 连接服务器。已验证任务运行成功；本机副本保留 60 天。
- 本机目录：`/Users/fan/Code/SelfSystem/.local/backups`。`latest.tar.gz` 为最近一次成功下载的副本，下载中断不会替换它。
- 已执行一次实际恢复：解压异地副本，校验 SQLite 完整性、记录版本、设备登记和 JSON 内容均通过。测试记录已按精确编号清理，随后重新备份；其他内容保留。
- 签发资料另在 `.local/access`，不在服务器备份中。迁移、恢复新设备时应同时保留这个私有目录。备份文件含个人内容和运行密钥，不应公开上传。

## 验收结果

- 无客户端证书：无法进入网站。四份正式证书：均通过实际 HTTPS 请求验证，服务端证书链验证正常。
- 临时设备：登记后可访问，撤销后下一次请求返回拒绝；临时登记已清理。
- 线上创建、修改、幂等重试、跨证书读取、冲突拒绝、回收站恢复、导出：通过。
- 只重启 Me 应用后，数据与版本保持完整；其他服务继续运行。
- 本地正式构建：关闭服务后重新打开页面，离线新增；服务恢复后自动上传一条记录，未重复创建。
- 393 像素手机布局：页面无横向溢出。电脑与手机视图已检查。
- 自动化检查 8 项通过，生产构建通过；依赖审计未报告已知漏洞。

## 仍需设备上的实际操作

四份证书已经生成并登记，但本报告不代表四台真实设备均已完成安装；本机钥匙串自动导入也未确认成功。首次使用按私有说明安装证书，不需要创建网站密码。

服务端提醒、重试和完成后停止发送已实现并测试；未完成你的安卓手机（以及后续若需使用的 iPhone）在锁屏、移动网络和 Wi-Fi 下的真实送达验收。需要在该手机开启通知并发送测试。PWA 安装状态、浏览器推送服务可达性与系统省电设置都会影响结果。

普通可导出的客户端证书代表“持有相应私钥”，不是不可复制的硬件身份；他人若复制到私钥也能访问，直到证书被撤销。撤销不能远程抹除设备已下载的离线内容。

首版没有重复任务、附件、系统日历同步、原生手机应用、周/月网格日历或 AI 功能。产品评估中的这些后续方向不属于本次已交付能力。

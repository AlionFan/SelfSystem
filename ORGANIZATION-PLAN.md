**Me 组织协作与任务管理方案 · v1.0**

编写日期：2026-09-06。状态：产品与实施建议，尚未开发或上线。基于现有 Me 个人空间、邮箱账号、四象限、看板、甘特图、Qwen 整理、语音输入与 PushPlus 提醒设计。当前按 10～50 人、小型企业或项目团队起步；这是方案假设，待实际团队规模确认后调整。

**1. 产品目标。** 一个人继续拥有自己的私人空间，同时可以加入多个组织。管理者分配工作，员工自主登记工作，双方在同一份任务上协作；组织可以按项目、部门和负责人统一查看进度。个人首页聚合“我需要做的事”，组织首页呈现“团队正在推进的工作”。

“组织”是协作空间，可以代表企业、工作室或团队。创建组织不等于完成企业资质认证，不显示未经验证的认证标识。待办与任务采用同一种数据：简短事项直接创建，复杂事项再展开项目、协作者、验收等字段。

**2. 先明确内容归属，再决定谁能看到。** 推荐以下三种模式，不能只增加一个“企业可见”开关后让管理员任意修改个人记录。

| 模式 | 使用场景 | 谁能修改 | 能否撤销企业访问 | 是否计入正式任务统计 |
| --- | --- | --- | --- | --- |
| 个人私密 | 私人想法、生活待办、未提交的工作草稿 | 本人 | 默认没有企业访问 | 否 |
| 个人分享，只读 | 希望让主管或同事了解一件个人维护的事项 | 本人；接收者不能重新分配或验收 | 本人可撤销后续访问 | 否，放在“员工分享”中单列 |
| 组织任务 | 管理者下发，或员工主动提交给组织的工作 | 按组织角色和任务关系授权 | 发布后不能单方变回私密；可申请取消 | 是 |

个人分享默认发布“当前选定内容的快照”，不持续同步之后的私人修改。作者可以手动更新分享版本；撤销后清理在线访问与缓存引用，但无法收回对方已下载、截图或已收到的通知。

个人记录进入正式工作流程时，使用“生成组织任务”：先选择要提交的正文、日期等内容，再创建独立的组织任务。私人原文继续保留，原始语音、AI 原文、历史版本和私人关联记录不会一起公开。两者后续不自动同步，避免私人补充内容意外进入组织。

直接在组织里创建的任务只有一份。它出现在组织看板和负责人的“我的任务”中时使用同一条记录；进度、评论和日期同步变化，不给每个人复制一份待办。

**3. 组织创建与成员体系。** 已验证邮箱的用户可创建组织，填写名称、简称、默认时区及任务偏好；创建者成为组织所有者。首次使用时不强制创建部门或项目，小团队可直接协作。

组织所有者或管理员通过指定邮箱发出邀请，邀请有效期建议七天、单次使用、可撤销。接收者用对应的已验证邮箱登录并主动加入；同邮箱后缀不自动获得企业访问权。一个账号可以加入多个组织，在每个组织拥有独立角色。

部门用于管理人员和管理范围，项目用于组织具体工作。首版支持简单部门、一个主要所属部门、一个或多个项目；项目可以跨部门。后续再加入多级部门、多重汇报关系和外部访客。

**4. 角色与权限。** “能看到”“能编辑”“能分配”“能验收”分别判断。现有系统的网站主账号角色，不能直接当作所有企业的管理员角色。

| 角色 | 可查看的组织内容 | 主要操作 | 边界 |
| --- | --- | --- | --- |
| 组织所有者 | 本组织全部正式任务，以及按分享授权可见的内容 | 组织设置、任命管理员、转让所有权、组织导出与解散 | 产品权限不能读取成员私人空间；只有所有者能转让或解散组织 |
| 组织管理员 | 本组织全部正式任务，以及按分享授权可见的内容 | 成员与项目管理、全组织分配、调整、验收、授权范围内导出 | 不能操作其他组织，不能自行取得私人记录 |
| 部门／项目负责人 | 明确授权管理范围内的任务；另可阅读明确分享给自己的内容 | 向该范围内成员派发工作、改派、调整期限、验收 | “员工的主管”不等于能管理该员工在其他项目或组织的任务 |
| 普通成员 | 自己参与的任务，以及按部门、项目或全组织开放的内容 | 创建任务、更新执行进展、补充说明、评论、提交完成、申请调整 | 看得见不代表能编辑；默认不能给其他人派任务或自行提高权限 |

组织任务的基础可见人员为负责人、创建者、协作者及有权管理该任务的管理者。可扩大到“项目成员”“部门成员”“全组织成员”；未明确扩大的任务默认仅上述人员可见。所有者和组织管理员具有全组织正式任务的管理可见性，界面要明确展示这一点，避免把“参与人可见”误认为对组织管理员保密。

组织任务的实际可见范围由组织、项目/部门和任务权限共同计算。任务若需要缩小到更敏感的范围，应调整所属项目或授权范围，首版不提供含义不明确的层层例外。任务参与者移除、项目退出、部门调动都触发权限重算；“创建过任务”仅保留历史署名，不提供离组后的永久访问。

个人只读分享使用独立授权，组织管理员身份本身不扩大该分享的接收范围。任务详情显示清楚的“可见人员”和可执行操作；只有有权管理任务的人能扩大范围。

**5. 一个任务需要哪些信息。** 快速入口只要求标题或正文；创建企业任务还要确认组织。其他内容渐进展开，沿用现有“默认／想法／待办／日程”与语音入口。

| 信息组 | 字段与建议 |
| --- | --- |
| 归属 | 个人或某个组织；组织任务可选项目、管理部门 |
| 工作内容 | 标题、正文、标签、验收标准；复杂工作可拆子任务 |
| 人员 | 创建者、分配者、唯一主要负责人、可选协作者、可选验收人 |
| 时间 | 计划开始、截止、预计投入时长、实际开始与完成时间；日期与时分仍允许分别填写 |
| 执行 | 状态、阻塞说明、完成说明、可选预计剩余时间 |
| 权限与提醒 | 可见范围、是否需要接收确认、是否需要验收、通知偏好 |
| 追溯 | 来源、版本、分配记录、变更记录、验收及重新打开记录 |

可以暂时没有负责人，进入组织的“待分派”列表；正式派发时必须明确一位主要负责人。多人共同承担时保留一个主负责人，其他人作为协作者。若每个人都要独立交付，则拆成子任务，各自有负责人和期限。

未发布草稿仅作者可见，发布前不进入企业正式统计或发出任务分配通知；成员停用后不能继续向该组织发布。员工可维护执行进度、实际时间、阻塞原因和补充说明；正式任务的负责人、承诺截止、验收标准和可见范围，由管理者修改或批准变更申请。员工自己的组织草稿可自由修改；发布为组织任务后适用相同管理规则。

**6. 三条主要使用流程。**

管理者派发：进入组织或项目 → 输入文字或语音 → AI 整理为草稿 → 确认负责人、期限、范围及验收要求 → 发布 → 负责人收到站内通知及已开启的微信提醒 → 执行、反馈、完成或提交验收。

员工自主登记：在个人入口选择“保存到某组织”，或在组织内新建 → 默认负责人为本人 → AI 整理后确认发布 → 自动进入组织列表和本人任务。一般工作不要求主管逐条批准创建；涉及团队承诺或需要验收时再启用相应流程。

私人内容用于协作：私人记录 → 选择“只读分享”或“生成组织任务” → 预览实际公开内容与接收范围 → 发布。管理者希望接管只读分享时可提出转为正式任务的请求，由作者确认选择内容后生成。

跨项目分配需要同时拥有任务管理权和目标范围的分配权；同名人员必须由用户选择，不能仅凭姓名自动匹配。跨组织转移不作为首版普通操作，避免不经确认复制企业资料。

**7. 任务状态与交付闭环。** 草稿发布后进入“待开始”，正常流转为“待开始 → 进行中 → 待验收 → 已完成”。不需要验收的任务，可以从执行状态直接完成。取消进入“已取消”；归档作为后续收纳操作，不当作另一种完成。

“阻塞”和“逾期”是额外标记，能够同时存在于进行中或待验收任务上。遇到阻塞时填写原因和需要谁协助，便于管理者处理；不要求员工反复调整虚假的完成百分比。

简单待办默认不强制“接单”，派发即可执行；重要任务可以要求确认接收。确认接收独立于执行状态，待接收任务有单独过滤项，避免所有小任务都增加一次点击。成员可反馈需要澄清、时间冲突或申请改派，管理者保留决定及调整记录。

需要验收的任务，员工点击“提交完成”并说明交付结果；验收人选择通过或退回并说明原因。提交与验收分别记录时间；看板拖拽、列表勾选、手机快捷操作和微信按钮必须遵循同一规则，不能从其他入口跳过验收。本人不能验收自己的交付，必要时由另一名有管理权限的成员接手。

**8. 页面与日常使用体验。** 在现有侧栏顶部增加空间切换：“个人空间／组织 A／组织 B／创建或加入组织”。个人空间的记录入口默认仍为个人，组织内创建默认归当前组织，并始终显示明显的组织标识。

“我的任务”聚合个人待办和各组织分配给自己的任务，用标签区分来源，可以按组织筛选。组织方的列表与统计不读取个人私密事项，也不显示其数量。个人的“今天做什么”、个人四象限排序可以是私人偏好，不会因为个人拖动而改变企业统一优先级。

组织侧栏首版采用“工作台、全部任务、项目、成员、组织设置”。点击任务打开侧边详情，依次呈现正文、执行与分配信息、讨论、变更记录；高级字段按需展开。手机优先显示“分给我、我创建、待我处理”，管理者另有“团队动态”，批量排程主要在电脑完成。

**9. 企业展板与可视化。** 所有视图展示同一份组织任务数据，切换视图不创建副本；筛选条件一致，按权限返回数据和统计，不能通过图表或计数推断无权访问的事项。

| 视图 | 解决的问题 | 主要交互与约束 |
| --- | --- | --- |
| 任务列表 | 快速找到和批量管理工作 | 筛选部门、项目、负责人、来源、状态、截止；批量操作先预览影响，逐项校验权限 |
| 任务看板 | 看清工作处于哪一步 | 待开始、进行中、待验收、已完成；可按人或项目分组，拖动遵守验收规则 |
| 甘特图 | 查看排程、交付节点和时间冲突 | 周/月缩放，按项目/负责人分组；无开始或结束信息的事项放“待排程”，不猜时长 |
| 四象限 | 排清重要性和紧急程度 | 组织重要性由有权人员确认；紧急度可结合截止辅助判断，无依据留待分配 |
| 成员工作量 | 发现过载与无人接手的工作 | 待办数、预计投入、到期分布、已阻塞任务；缺少时长明确标记，不把一项任务当作固定工时 |
| 日历 | 查看会议和按日安排 | 任务截止、日程分开显示；不自动把任务时间段变成所有员工的会议 |

工作台优先突出“待分派、今日到期、已逾期、被阻塞、待验收”，每个数字可点击进入对应列表。项目进度按任务完成数计算时明确标注计算方式；没有时长的任务不能假装得到准确的工时完成率。

建议提供：当前逾期数、按期提交率、按期验收率、待验收时长、任务平均周期、阻塞原因分布。保留原截止和修改历史，不能通过不断延期美化数据。员工自行创建与管理者分配的任务分来源筛选，但使用同一执行流程。

不建议第一版做任务数量排名或自动绩效评分。任务颗粒度、难度和依赖差异很大；这些图表更适合帮助排程与协调，而不是直接认定员工表现。

**10. AI 与语音在组织中的边界。** 保留 Qwen-Flash 和现有语音转写，帮助提取类别、标题、日期、工作拆分及负责人建议。组织发布前先展示整理结果，由人确认；发布后的正文和正式安排仅提供修改建议，不让后台 AI 自动改动已派发承诺。

例如输入“下周三之前让小张完成报价，我来审核”，AI 可以建议截止日期、候选负责人和验收人，但必须由操作者确认。同名、权限不足、成员已退出、日期不明确时提示补充；AI 不得自行加入成员、扩大可见范围、跨组织检索或执行验收。

组织需独立设置是否允许把工作内容发送给百炼，以及是否允许语音转写。个人账号已经开启 AI，不代表自动授权发送组织资料。组织关闭外部 AI 时，组织上下文里的 AI 和云端语音入口相应停用，普通文字记录继续可用。

AI 只能处理操作者有权访问且本次选中的内容；不读取成员私人笔记，组织总结也必须先按权限筛选。AI 任务执行与应用结果时再次检查成员资格、范围和版本；期间发生撤权或人工修改则不应用。组织数据被分享或提升可见范围时，需要同步检查摘要、历史、原文、评论与子任务，不能只检查正文。

费用按个人与组织分别记账，再受平台总预算约束。创建多个组织不应自动增加整个平台的可用费用额度。具体额度在团队规模和实际调用量确认后配置，继续保留达到限额时可正常手动记录的退路。

**11. 站内消息、微信提醒和回执。** 站内待办与通知作为工作状态的可靠入口，PushPlus 作为可选提醒渠道。员工主动连接自己的通知账号；组织是否允许外发与员工是否开启该组织通知都通过后，才能发送组织提醒。

首版通知覆盖任务分配/改派、确认接收要求、截止临近、逾期、被提及、阻塞求助、提交验收、验收通过或退回。普通进度变更合并进摘要，不每次编辑都发微信；提供免打扰时间、每日摘要、按任务静音和合并去重。

企业通知默认使用“某组织有一项任务待处理”的简短提示和受登录保护的链接；是否显示任务标题由组织设置决定，正文不默认整段推送。即使退出组织后再次打开旧提醒，也必须重新检查权限；未发送的通知随撤权取消。

明确区分：通知服务已受理、平台报告投递成功、用户已知晓、用户确认接收任务、用户提交完成、管理者验收通过。PushPlus 的结果回调用于投递状态，不能据此推断本人已读或任务完成；其 API 文档明确区分受理与发送结果。[PushPlus 消息接口](https://www.pushplus.plus/doc/guide/api.html)

微信中的动作实际进入 Me 页面，并要求登录后由本人点击。对需要验收的任务，“完成”应呈现为“提交验收”。已有个人提醒的“稍后提醒”只调整本人的通知时刻，不能偷偷改变组织任务截止；调期限应走任务变更。后续可评估企业微信应用通知，独立于首版协作功能安排。

**12. 人员变动与数据交接。** 成员状态包括邀请中、正常、停用、已退出。停用或移除立即撤销该组织的服务器访问权、组织导出权和未发送通知；不注销其个人账号，不影响其其他组织。

离职交接先汇总未完成任务、待验收事项和担任负责人项目，支持批量改派。紧急停用不能被“还有任务没交接”阻挡：先撤权，未交接事项进入组织的“待交接”列表，由有权管理者处理。历史评论、完成记录和署名保留，显示“已离开组织”。

不能移除最后一位组织所有者；转让需另一位正常成员明确接收。部门移动不自动将历史任务全部搬到新部门；项目角色与任务归属分别调整并留痕。组织关闭先停用和导出，再按明确的保留/删除规则处理；首版不做一键不可恢复清空。

企业导出只覆盖申请者有权导出的组织任务，不混入个人空间。只读分享默认不进入组织批量导出。个人导出不顺带带走全部企业资料；导出组织任务需独立判断组织授权。

**13. 离线与多人同时操作。** 个人空间保留现有离线能力；组织首版建议在线操作，暂不把企业正文长期缓存到本机离线数据库。仍可在个人私密草稿里离线记下想法，联网后明确选择提交到组织。

服务器无法收回已经下载或截图的内容，因此不承诺离职后能远程擦除所有设备。后续若增加组织离线功能，必须有组织开关、受控缓存时效、账号与组织双重隔离，以及恢复联网时的撤权清理。

同一任务保留版本号。两人同时改负责人、截止或状态时，后提交者看到冲突提示，不静默覆盖。评论作为追加内容处理；任务发布、分配、状态变更、通知事件在同一事务中保存，网络重试以操作编号去重，避免重复任务或重复通知。

**14. 对现有系统的改造。** 当前记录查询和保存主要按 `user_id` 隔离，普通修改一次提交整份记录。组织版需要加入“内容归属、组织成员资格、角色范围、任务参与关系”的统一权限判断，不能直接允许所有管理员调用其他用户的个人接口。

| 新增或调整对象 | 用途 |
| --- | --- |
| 组织、组织成员 | 保存组织设置、成员状态和组织内角色 |
| 部门、项目、管理授权 | 明确人员范围、工作归属和跨部门管理范围 |
| 工作条目 | 明确个人/组织归属，创建者与负责人分离 |
| 任务参与者、只读分享 | 协作关系与选定内容的分享版本 |
| 分配、验收和变更请求 | 支持接收确认、改派、期限调整、验收闭环 |
| 评论、操作历史 | 记录工作讨论、前后变化、操作者与时间 |
| 通知事件与投递 | 将业务状态与外部消息投递分开，允许重试和取消 |
| AI 与语音用量 | 按组织计费归属，同时受平台总限额控制 |

统一的授权层覆盖列表、详情、统计、搜索、导出、历史、评论、关联记录、AI、通知和未来附件下载。组织标识不能只信任前端传入的值，每次请求都查当前有效关系；默认拒绝，权限不足不返回隐藏正文。这一设计依据 OWASP 的最小权限与逐请求授权原则。[OWASP 授权指南](https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html)

组织任务采用明确动作接口，例如分配、提交完成、验收、变更范围；员工的进度更新不能通过提交整份 JSON 顺便改掉负责人或权限。个人四象限/今日安排与组织统一优先级分开保存，解决不同视图互相覆盖的问题。

首轮原型可使用现有 SQLite 验证流程。正式多人协作版本建议评估迁移 PostgreSQL，以便扩展事务、并发和数据库层隔离；这是架构建议，不应把某个固定人数当作 SQLite 的容量上限。若采用行级安全，应用账户不能使用超级用户、BYPASSRLS 或默认绕过策略的表所有者；数据库策略也不能替代应用动作权限检查。[PostgreSQL 行级安全说明](https://www.postgresql.org/docs/current/ddl-rowsecurity.html)

继续沿用 me.joybeat.cn、React/Express 和 Coolify 的单应用部署。暂不拆微服务，不新增另一套账号系统；附件上传和企业微信接入放到后续阶段。上线前实际检查当前服务器资源和并发，保护其他已有服务。

**15. 开发顺序与首版范围。**

| 阶段 | 交付内容 | 完成标准 |
| --- | --- | --- |
| 权限与数据基础 | 内容归属、组织与成员、限定邮箱邀请、基本角色、权限预览、个人数据兼容 | 两个组织与私人空间互不串数据，旧账号正常使用 |
| 协作首版 | 只读分享/生成组织任务、简单部门与项目、指派、自建、评论、接收与可选验收、改派交接 | 管理者和员工完整走通创建到完成，同一任务同步显示 |
| 统一展板与提醒 | 列表、看板、现有四象限及甘特图接入组织数据、基本管理统计、站内通知、PushPlus 回执 | 视图口径一致，权限即时生效，通知不替代业务状态 |
| 后续增强 | 多级部门、依赖与里程碑、工作量排程、日历、附件、周期任务、访客、企业微信、经审核的 AI 周报 | 按真实团队反馈分批引入，不先做复杂审批或绩效系统 |

首个可投入使用的组织版本应完成前三个阶段，不能只发布组织名称和分配按钮。工作量预测、复杂甘特依赖和全套人事系统不作为首版阻塞条件。小范围试用后再决定扩容与数据库迁移时点，不在缺少部署数据时承诺固定工期和容量。

**16. 上线验收。** 首版至少通过以下业务场景，而不仅是页面能打开：

1. 加入组织后，主管看不到员工既有私人记录、私人数量或 AI 历史。
2. 只读分享只公开选定快照；后续私人修改不外泄；撤销后所有在线入口失效。
3. 员工自建组织任务和主管派发任务都进入组织展板，并可按来源筛选。
4. 同一任务在员工和管理者视图中进度一致，快速重复提交不产生副本。
5. 普通成员即使手工修改请求，也不能改负责人、扩大范围或跨组织读取。
6. 部门/项目负责人只能管理授权范围，调动与跨项目角色变更即时更新权限。
7. 需要验收的任务无法通过勾选、拖拽、旧微信链接或 API 绕过验收。
8. 离职停用先撤权，尚未交接的任务可由管理员继续分配；个人空间保持正常。
9. 两人同时调整期限或负责人会提示冲突，不丢失另一人的修改。
10. PushPlus 投递成功不触发已读或完成；延后个人提醒不修改组织截止。
11. 组织关闭外部 AI 后，新语音与 AI 调用停止；撤权后迟到的 AI 结果不会应用。
12. 私人导出、组织导出、图表统计、任务历史及通知链接都遵循同一权限边界。
13. 所有旧数据仍保留个人归属，登录、语音、AI、提醒和备份恢复可正常使用。
14. 电脑与手机分别验收派发、反馈、完成、验收和权限预览，不以桌面截图代替手机实测。
15. 发布前在与正式环境相近的资源限制下完成并发与恢复测试，确认现有其他服务不受影响。

**17. 迁移与维护。** 数据升级先补默认个人归属，不把现有记录自动公开，也不自动把网站主账号升级成所有组织的管理者。发布前保留完整备份，并在测试副本上演练恢复；组织数据出现后需要同步升级组织导出、通知撤权和恢复逻辑。

现服务器于 2027-06-16 00:18:22（北京时间）到期，建议 2027 年 5 月前完成迁移演练。组织版需要一起迁移账号、成员关系、任务、讨论、审计、通知配置、AI 配置和后续附件；保持域名入口不变。数据库升级或回滚不能以覆盖旧备份的方式丢弃新产生的企业工作记录。

**18. 建议采用的默认决策。** 个人内容默认私密；“只读分享”和“生成组织任务”分别呈现；组织正式任务一位主要负责人，可有协作者；普通任务不强制接收，验收按任务开启；组织任务默认向参与者和有权管理者可见，全员查看需要明确设置；管理者可按范围统一处理组织任务；个人优先级和企业优先级分别维护；组织 AI 发布前确认；微信只做通知入口；组织首版在线使用。

待进一步确定的参数为实际团队规模、是否需要多级部门，以及组织是否允许把工作内容发给百炼和 PushPlus。这些参数不会改变个人私密空间与组织正式任务分开的基础设计。
